package grp23
@Grab(group='org.mongodb', module='mongodb-driver-sync', version='4.10.1') // Use the appropriate MongoDB driver version
import com.mongodb.client.MongoClients

/**
 * Todo:
 * Creating Database:
 * 	- Check if the database exists or not
 * 		- If exists then do not create the database
 * 		- Else create the database
 * 	- A db will only be created by adding collections into it.
 * 	- 
 */


/**
 * 
 */
